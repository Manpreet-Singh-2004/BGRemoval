# 1. Use a lightweight, official Python base image
FROM python:3.12-slim

# 2. Prevent Python from writing .pyc files and buffer issues
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# 3. Set the working directory inside the container
WORKDIR /app

# 4. Install essential system dependencies for image processing
# rembg relies on OpenCV and ONNX runtime, which need these system libraries to avoid the dreaded "libGL.so.1" error.
RUN apt-get update && apt-get install -y \
    libgl1 \
    libglib2.0-0 \
    && rm -rf /var/lib/apt/lists/*

# 5. Copy just the requirements first to cache the pip install step
COPY requirements.txt .

# 6. Install the Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# 7. Copy the rest of your application code
COPY . .

# 8. Expose the port the app runs on
EXPOSE 8000

# 9. Start the FastAPI server using Uvicorn
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]